import { StyleSheet } from "react-native";


export default StyleSheet.create({
    loadingStyles:{ 
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      },
    })    